# Moore-Penrose pseudo-inverse of a matrix

Moore-Penrose pseudo-inverse of a matrix

## Usage

``` r
pinv(x, tol = -1)

ginv.egpd(x, tol = sqrt(.Machine$double.eps))
```

## Arguments

- x:

  a matrix

- tol:

  a scalar tolerance

## Value

A matrix
