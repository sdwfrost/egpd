# Log-likelihood from a fitted `egpd` object

Log-likelihood from a fitted `egpd` object

## Usage

``` r
# S3 method for class 'egpd'
logLik(object, ...)
```

## Arguments

- object:

  a fitted `egpd` object

- ...:

  not used

## Value

A `logLik` object
