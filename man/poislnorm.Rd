% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/countfams.R
\name{poislnorm}
\alias{poislnorm}
\alias{dpoislnorm}
\alias{ppoislnorm}
\alias{qpoislnorm}
\alias{rpoislnorm}
\title{Poisson-lognormal distribution}
\usage{
dpoislnorm(x, mu = 1, sigma = 1, log = FALSE)

ppoislnorm(q, mu = 1, sigma = 1, lower.tail = TRUE, log.p = FALSE)

qpoislnorm(
  p,
  mu = 1,
  sigma = 1,
  lower.tail = TRUE,
  log.p = FALSE,
  max.value = 1e+06
)

rpoislnorm(n, mu = 1, sigma = 1)
}
\arguments{
\item{x, q}{vector of counts}

\item{mu}{mean, \code{mu > 0}}

\item{sigma}{standard deviation of the log-intensity, \code{sigma > 0}}

\item{log, log.p}{logical; return the log density / probability}

\item{lower.tail}{logical; if TRUE probabilities are \code{P[X <= x]}}

\item{p}{vector of probabilities}

\item{max.value}{largest count searched when inverting the cdf}

\item{n}{number of draws}
}
\value{
a numeric vector
}
\description{
\code{Y | Z ~ Poisson(exp(Z))} with \code{Z ~ N(muz, sigma^2)} and
\code{muz = log(mu) - sigma^2 / 2}, so \code{E[Y] = mu}. The pmf has no closed form and
is evaluated by adaptive Gauss-Hermite quadrature centred on the mode of the integrand.
The tail is lognormal-like: heavier than the negative binomial's, but still in the
Gumbel domain (\code{xi = 0}), so this family models a heavy body rather than a heavy
tail.
}
