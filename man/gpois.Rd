% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/countfams.R
\name{gpois}
\alias{gpois}
\alias{dgpois}
\alias{pgpois}
\alias{qgpois}
\alias{rgpois}
\title{Generalized Poisson distribution}
\usage{
dgpois(x, mu = 1, lambda = 0.5, log = FALSE)

pgpois(q, mu = 1, lambda = 0.5, lower.tail = TRUE, log.p = FALSE)

qgpois(
  p,
  mu = 1,
  lambda = 0.5,
  lower.tail = TRUE,
  log.p = FALSE,
  max.value = 1e+06
)

rgpois(n, mu = 1, lambda = 0.5)
}
\arguments{
\item{x, q}{vector of counts}

\item{mu}{mean, \code{mu > 0}}

\item{lambda}{dispersion, \code{lambda} in \code{[0, 1)}; \code{lambda = 0} is Poisson}

\item{log, log.p}{logical; return the log density / probability}

\item{lower.tail}{logical; if TRUE probabilities are \code{P[X <= x]}}

\item{p}{vector of probabilities}

\item{max.value}{largest count searched when inverting the cdf}

\item{n}{number of draws}
}
\value{
a numeric vector
}
\description{
Consul & Jain's generalized Poisson in the mean parameterisation:
\code{theta = mu (1 - lambda)}, so \code{E[Y] = mu} and
\code{Var[Y] = mu / (1 - lambda)^2}. The tail decays geometrically, so this family
captures overdispersion but not a heavy tail.
}
